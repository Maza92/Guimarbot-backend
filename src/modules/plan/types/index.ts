export enum PlanStatus {
  Active = 'active',
  Inactive = 'inactive',
  Deleted = 'deleted',
}

export enum ReferralStatus {
  Pending = 'pending',
  Accepted = 'accepted',
  Rejected = 'rejected',
}
